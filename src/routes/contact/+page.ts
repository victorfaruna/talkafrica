import type { PageLoad } from './$types';

export const load: PageLoad = () => {
    return {
        title: "Contact Us | TalkAfrica",
        description: "Get in touch with TalkAfrica. We'd love to hear from you — whether you have a story, a question, or just want to say hi."
    };
};
